extends layouts/main

block content
    .form_container
        form(action="/login", method="post")
            input(type="email", name="email", placeholder="email")
            input(type="password", name="password", placeholder="password")
            input(type="sunmit", value="Log In")